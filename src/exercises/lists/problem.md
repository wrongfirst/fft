We looked at tuples, now, lets look at another way of grouping related things together: lists. 

The key difference between tuples and lists is that lists are ordered. Lists in OCaml are written as 

```ocaml
let x = [1; 2; 3]
```
Also, an empty list is written as `[]`. 